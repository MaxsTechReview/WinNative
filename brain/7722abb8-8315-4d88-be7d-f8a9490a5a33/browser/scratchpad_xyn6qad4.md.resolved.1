# Task: Find Mono and Gecko installation code in GameNative repository

## Plan
1. Open https://github.com/utkarshdalal/GameNative
2. Search for "mono" and "gecko" in the repository.
3. Look for files: `XServerScreen.kt`, `WineUtils.kt`.
4. Analyze how they handle `msiexec` and installation of mono/gecko.
5. Extract code snippets and logic.

## Findings
- `XServerScreen.kt` and `WineUtils.java` don't seem to contain direct Mono/Gecko/msiexec installation logic based on initial searches for "mono" and "gecko".
- `wincomponents.json` lists various components but not Mono/Gecko.
- The repository issue #564 mentions "We currently have an offline mono installer." and requests an "offline gecko installer".
- `PreLaunchSteps.kt` and `LaunchDependencies.kt` exist but only show `GogScriptInterpreter` related steps/dependencies.
- Need to look for `msiexec` specifically in the whole codebase or files related to "Container" or "Component Manager".

# Tasks — All Complete ✅

## Phase 1–4 ✅
- [x] Menu cleanup, fragment NPE fixes, Stores routing, icon, filter panel

## Phase 5 — Tab Scroll Limit ✅
- [x] Tab row limited to `widthIn(max = 340.dp)`, ~4 tabs visible, rest scrollable

## Phase 6 — Game Launch ✅
- [x] `launchSteamGame()` with A: drive mounting via `Container.drivesIterator`
- [x] Shortcut-first fallback → container-based launch with .exe auto-detection

## Phase 7 — Artwork Fix ✅
- [x] Fallback chain: `getHeroUrl()` → `getHeaderImageUrl()` → `getCapsuleUrl()` → CDN `library_hero.jpg`

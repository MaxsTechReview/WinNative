[package]
name = "wn_libsteamclient"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
publish = false

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
jni = "0.21"
libc = "0.2"
ctor = "0.2"

[profile.release]
codegen-units = 1
lto = "thin"
strip = "symbols"
